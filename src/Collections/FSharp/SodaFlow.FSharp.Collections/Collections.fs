/// <summary>
///     The F# surface over SodaFlow's reactive collections, as SodaFlow.Async's MapAsync module is
///     over the async ones: curried, pipeline-friendly functions typed against F#'s native
///     <c>unit</c> and <c>option</c> rather than <c>SodaFlow.Functional</c>'s equivalents.
/// </summary>
/// <remarks>
///     Scaffold. Nothing is declared here yet — the collections are still to be brought over — but
///     an F# project cannot compile with no source file at all, so this one exists to give the
///     assembly, the package and its wiring somewhere to hang until it does.
/// </remarks>
module SodaFlow.Collections
