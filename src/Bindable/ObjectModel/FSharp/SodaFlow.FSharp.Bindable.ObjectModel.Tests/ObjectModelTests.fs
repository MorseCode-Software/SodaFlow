// An F# project needs at least one source file to compile, so this file exists to hold the
// namespace until there are tests to put in it. Replace it rather than adding beside it.
namespace SodaFlow.FSharp.Bindable.ObjectModel.Tests
