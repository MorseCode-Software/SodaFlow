<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <AssemblyName>SodaFlow.FSharp.Async.Tests</AssemblyName>
        <RootNamespace>SodaFlow.Async.Tests</RootNamespace>
        <TargetFrameworks>net10.0;net472</TargetFrameworks>
        <OutputType>Exe</OutputType>
        <!-- Never packed. Said here rather than inferred: what marked these as test
             projects was the reference to Microsoft.NET.Test.Sdk, which is gone. -->
        <IsPackable>false</IsPackable>
        <!-- TUnit's transitive dependencies do not claim net472; their netstandard2.0 assets work. -->
        <SuppressTfmSupportBuildWarnings>true</SuppressTfmSupportBuildWarnings>
        <!-- The usings are listed per file here, as everywhere else in this repository. -->
        <TUnitImplicitUsings>disable</TUnitImplicitUsings>
        <TUnitAssertionsImplicitUsings>disable</TUnitAssertionsImplicitUsings>
    </PropertyGroup>

    <!-- Microsoft.Testing.Platform builds a native apphost for net472, which needs an explicit RID. -->
    <PropertyGroup Condition=" '$(TargetFramework)' == 'net472' ">
        <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    </PropertyGroup>

    <ItemGroup>
        <Compile Include="AssemblyInfo.fs"/>
        <Compile Include="TestUtil.fs"/>
        <Compile Include="MapAsyncTests.fs"/>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="TUnit" Version="1.66.10"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\..\FSharp\SodaFlow.FSharp.Tests.Assertions\SodaFlow.FSharp.Tests.Assertions.csproj"/>
        <ProjectReference Include="..\..\..\FSharp\SodaFlow.FSharp\SodaFlow.FSharp.fsproj"/>
        <ProjectReference Include="..\..\SodaFlow.Core.Async\SodaFlow.Core.Async.csproj"/>
        <ProjectReference Include="..\SodaFlow.FSharp.Async\SodaFlow.FSharp.Async.fsproj"/>
    </ItemGroup>

</Project>
